# oauth-playground

> oauth-server: oauth服务
``` bash
基于spring-security搭建的oauth2认证服务
url:http://seac-playground-oauth.cloud.sh.edu.cn/
```
> oauth-client: 认证客户端(playground)
``` bash
基于springboot搭建的认证客户端
url:http://seac-playground.cloud.sh.edu.cn/
```
> oauth-resource: 资源服务
``` bash
基于springboot搭建的资源服务样例
url:http://seac-playground-resource.cloud.sh.edu.cn/
```


## 部署jar
``` bash
nohup java -jar **.jar >> *.log &
```

